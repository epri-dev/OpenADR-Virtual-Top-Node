<% content_for :subnav_items do %>
  <%= render 'application/menu_item_subnav_groups/vtn_config_subnav_items' %>
<% end %>

<%= generate_error flash[:error] %>
<%= generate_notice flash[:notice] %>

<%= generate_errors @target, "prohibited this Target from being saved:" %>

<%= generate_list(@targets, Target, "Targets",
    [:name, :type, :tag, :vens_count],
    new_target_path,
    "Create Target") %>
