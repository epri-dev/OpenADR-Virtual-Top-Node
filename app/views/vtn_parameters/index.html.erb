<% content_for :subnav_items do %>
  <%= render 'application/menu_item_subnav_groups/vtn_config_subnav_items' %>
<% end %>

<%= generate_notice notice %>

<%= generate_error flash[:error] %>

<%= generate_errors @vtn_parameters, "prohibited the parameters from being saved:" %>

<%= generate_edit_columns @vtn_parameters, VtnParameter, "VTN Parameters",
    [:vtn_name, :poll_interval, nil], "Update Parameters" %>

<%= generate_view_details_column @vtn_parameters, VtnParameter, "End Points",
    [:endpoint20a_url, :endpoint20b_url], false %>

<%= generate_edit_columns @vtn_parameters, VtnParameter, "Test VEN",
    [:ven_id, nil, nil], "Update Test VEN" %>