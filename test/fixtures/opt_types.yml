# == Schema Information
#
# Table name: opt_types
#
#  id          :integer          not null, primary key
#  name        :string(255)
#  description :string(255)
#  created_at  :datetime         not null
#  updated_at  :datetime         not null
#

optIn:
  name: "optIn"
  description: "optIn"

optOut:
  name: "optOut"
  description: "optOut"